import { LitElement, html, css } from "lit-element";

class AppDetail extends LitElement {
	static get properties() {
		return {
		};
	}
	render() {
		return html`app-detail</p>`;
	}
}
customElements.define("app-detail", AppDetail);
